# API 代理目标（开发模式 dev server 的 proxy target）
# 用户端 frontend 连接 backend，默认 http://localhost:8000
VITE_API_TARGET=http://localhost:8000

# ── 生产环境 API 地址（构建时注入） ──
# 方式一：同源部署（推荐）— 通过 nginx 反向代理 /api 到后端，无需设置此项
# 方式二：跨域部署 — 前端与后端不同域名时，设置后端完整 URL（不带尾部斜杠）
# 示例: VITE_API_BASE_URL=https://api.example.com
# VITE_API_BASE_URL=
