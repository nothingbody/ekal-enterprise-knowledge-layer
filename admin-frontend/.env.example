# API 代理目标（开发模式 dev server 的 proxy target）
# 管理后台 admin-frontend 连接 server，默认 http://localhost:8080
VITE_API_TARGET=http://localhost:8080

# 生产构建 API 前缀（与 deploy Nginx /api/central/v1 一致；也可在 docker-compose admin-build 里注入）
# VITE_ADMIN_API_BASE=/api/central/v1
