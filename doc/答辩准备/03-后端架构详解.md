# 后端架构详解

## 一、整体分层架构

```mermaid
graph TD
    subgraph API 路由层 - app/api/
        A1[auth.py 认证]
        A2[chat.py 对话]
        A3[knowledge_bases.py 知识库]
        A4[documents.py 文档]
        A5[models.py 模型配置]
        A6[... 共 24 个路由模块]
    end

    subgraph 业务服务层 - app/services/
        S1[chat_service 对话编排]
        S2[retrieval_service 检索服务]
        S3[embedding_service 向量化]
        S4[document_service 文档处理]
        S5[memory_service 用户记忆]
        S6[... 共 22 个服务模块]
    end

    subgraph 核心引擎层 - app/core/
        C1[llm_client LLM 统一调用]
        C2[vector_store/ 向量存储抽象]
        C3[context_engine/ 上下文管理]
        C4[tools/ Agent 工具框架]
        C5[parser 文档解析器]
        C6[chunking 文本切分器]
        C7[query_rewrite 查询改写]
        C8[channels/ 多渠道适配]
    end

    subgraph 数据模型层 - app/models/
        M1[User 用户]
        M2[KnowledgeBase 知识库]
        M3[Document/Chunk 文档/切片]
        M4[ChatConversation/Message]
        M5[ModelConfig 模型配置]
        M6[... 共 26 个 ORM 模型]
    end

    subgraph 基础设施层
        I1[(PostgreSQL/SQLite)]
        I2[(ChromaDB/PGVector)]
        I3[(Redis)]
        I4[Celery 任务队列]
    end

    A1 & A2 & A3 & A4 & A5 --> S1 & S2 & S3 & S4 & S5
    S1 & S2 & S3 & S4 & S5 --> C1 & C2 & C3 & C4 & C5 & C6 & C7
    C1 & C2 & C3 --> M1 & M2 & M3 & M4 & M5
    M1 & M2 & M3 & M4 & M5 --> I1
    C2 --> I2
    S1 --> I3
    S4 --> I4
```

---

## 二、请求处理流程

### 2.1 一次完整的 Chat 请求

```mermaid
sequenceDiagram
    participant Client as 前端
    participant API as api/chat.py
    participant CS as chat_service
    participant RS as retrieval_service
    participant ES as embedding_service
    participant VDB as 向量数据库
    participant LLM as LLM API

    Client->>API: POST /api/v1/chat/stream
    API->>API: JWT 认证 + 参数校验
    API->>CS: stream_chat(db, user_id, chat_req)
    
    CS->>CS: resolve_chat_mode()<br/>判断模式: rag/sql/agent/multi_agent
    CS->>CS: get_or_create_conversation()
    CS->>CS: get_chat_history()
    
    alt RAG 模式
        CS->>RS: retrieve(db, kb_ids, query, ...)
        RS->>RS: condense_question() 指代消解
        RS->>RS: generate_multi_queries() 多查询扩展
        
        loop 每个知识库
            RS->>ES: search_similar()
            ES->>VDB: 向量查询
            VDB-->>ES: 相似结果
            RS->>RS: bm25_search() 关键词检索
            RS->>RS: rrf_fusion() 融合排序
        end
        
        RS->>RS: rerank_documents() 重排序
        RS->>RS: expand_context_window()
        RS-->>CS: 检索结果列表
        
        CS->>CS: build_context() 构建上下文
    end
    
    CS->>LLM: chat_completion(messages, stream=True)
    
    loop 流式输出
        LLM-->>CS: token chunk
        CS-->>API: yield SSE event
        API-->>Client: data: {"content": "..."}
    end
    
    CS->>CS: save_chat_result() 保存消息
    CS->>CS: consume_tokens() 扣减配额
```

### 2.2 文档处理流程

```mermaid
flowchart TD
    A[用户上传文件] --> B[api/documents.py<br/>保存文件到 UPLOAD_DIR]
    B --> C{Celery 是否可用?}
    
    C -->|是| D[Celery Worker<br/>异步处理]
    C -->|否| E[同步处理<br/>桌面模式]
    
    D & E --> F[document_service<br/>process_document]
    
    F --> G[parser.py<br/>根据文件类型解析]
    G --> G1[parse_pdf]
    G --> G2[parse_docx]
    G --> G3[parse_excel]
    G --> G4[parse_pptx]
    G --> G5[parse_txt/md/html]
    G --> G6[OCR 图片识别]
    
    G1 & G2 & G3 & G4 & G5 & G6 --> H[纯文本内容]
    
    H --> I[chunking.py<br/>根据策略切分]
    I --> J[文本块列表]
    
    J --> K[embedding_service<br/>批量向量化]
    K --> L[vector_store<br/>写入向量数据库]
    
    J --> M[写入 DocumentChunk 表<br/>保存原文和元数据]
    
    L & M --> N[更新 Document 状态<br/>COMPLETED]
```

---

## 三、核心服务模块详解

### 3.1 对话服务 (chat_service.py)

```mermaid
graph TD
    CS[chat_service] --> |拆分为 4 个子模块| SUB
    
    subgraph SUB[子模块结构]
        CC[chat_constants.py<br/>Prompt 模板和常量]
        CH[chat_helpers.py<br/>错误分类、截断、上下文构建]
        CV[chat_conversation.py<br/>对话 CRUD 和历史管理]
        CA[chat_agent.py<br/>Agent / 多Agent / 斜杠命令]
    end
    
    CS --> MODE{对话模式路由}
    MODE --> |知识库问答| RAG[RAG 模式<br/>检索 + 生成]
    MODE --> |数据库查询| SQL[SQL 模式<br/>Text-to-SQL]
    MODE --> |工具调用| AGT[Agent 模式<br/>Function Calling]
    MODE --> |协作分发| MAG[多 Agent 模式<br/>路由 + 汇总]
    MODE --> |混合| HYB[混合模式<br/>RAG + SQL]
```

**五种对话模式**：

| 模式 | 触发条件 | 流程 |
|------|----------|------|
| **RAG** | 关联知识库 | 检索知识库 → 构建上下文 → LLM 生成 |
| **SQL** | 关联数据库源 | 自然语言 → SQL → 执行 → 解读结果 |
| **Agent** | 启用 Agent 模式 | LLM 决定调用工具 → 执行 → 返回 |
| **Multi-Agent** | 配置多 Agent | 路由分发子问题 → 各 Agent 处理 → 汇总 |
| **Hybrid** | 同时关联 KB + DB | RAG + SQL 并行检索 → 融合生成 |

### 3.2 检索服务 (retrieval_service.py)

```mermaid
flowchart LR
    subgraph 输入
        Q[用户查询]
        KB[知识库列表]
    end
    
    subgraph 查询预处理
        CD[condense_question<br/>指代消解]
        MQ[generate_multi_queries<br/>多查询扩展]
    end
    
    subgraph 检索执行
        VS[向量检索<br/>search_similar]
        BM[BM25 检索<br/>bm25_search]
        RRF[RRF 融合<br/>rrf_fusion]
    end
    
    subgraph 后处理
        RK[重排序<br/>rerank_documents]
        EX[上下文扩展<br/>expand_context_window]
        FT[过滤失效文档<br/>filter_results]
    end
    
    Q --> CD --> MQ
    MQ --> VS & BM
    VS & BM --> RRF
    RRF --> FT --> RK --> EX
```

### 3.3 Agent 工具框架 (core/tools/)

```mermaid
classDiagram
    class BaseTool {
        <<abstract>>
        +name: str
        +description: str
        +parameters: dict
        +execute(args) ToolResult
    }
    
    class ToolResult {
        +success: bool
        +data: Any
        +error: str
    }
    
    class ToolRegistry {
        +register(tool)
        +get_tool(name) BaseTool
        +list_tools() List
        +to_openai_functions() List
    }
    
    BaseTool <|-- KnowledgeSearchTool
    BaseTool <|-- SQLQueryTool
    BaseTool <|-- CalculatorTool
    BaseTool <|-- CurrentTimeTool
    BaseTool <|-- WebSearchTool
    BaseTool <|-- AgentQueryTool
    BaseTool <|-- BrowserTool
    BaseTool <|-- CodeExecutorTool
    
    ToolRegistry --> BaseTool : 管理
    BaseTool --> ToolResult : 返回
```

**8 个内置工具**：

| 工具名 | 功能 | 使用场景 |
|--------|------|----------|
| **knowledge_search** | 搜索知识库 | 回答需要查阅文档的问题 |
| **sql_query** | 执行 SQL 查询 | 查询结构化数据 |
| **calculator** | 数学计算 | 精确数值计算 |
| **current_time** | 获取当前时间 | 时间相关问题 |
| **web_search** | 网络搜索 | 查找实时信息 |
| **agent_query** | 调用其他 Agent | 多 Agent 协作中的子任务分发 |
| **browser_tool** | 网页内容提取 | 读取指定 URL 内容 |
| **code_executor** | 沙箱执行代码 | 运行 Python 代码 |

---

## 四、LLM 调用统一层 (llm_client.py)

```mermaid
graph TD
    subgraph 调用入口
        CC[chat_completion<br/>对话生成]
        EM[get_embedding<br/>文本向量化]
        RR[rerank_documents<br/>结果重排]
    end
    
    subgraph 客户端池
        BP[_build_client<br/>连接池管理]
        PP[_CLIENT_POOL<br/>复用 AsyncOpenAI<br/>最大 16 个客户端]
    end
    
    subgraph 模型提供商
        OA[OpenAI<br/>GPT-4/3.5]
        OL[Ollama<br/>本地模型]
        AN[Anthropic<br/>Claude]
        CT[其他 OpenAI 兼容 API]
    end
    
    CC & EM & RR --> BP
    BP --> PP
    PP --> OA & OL & AN & CT
```

**关键设计**：
- **连接池复用**：按 (base_url, api_key_hash) 缓存客户端，避免重复创建连接
- **API Key 加密存储**：数据库中存密文，调用时解密
- **统一接口**：所有 LLM 通过 OpenAI SDK 的兼容协议调用

---

## 五、安全机制

```mermaid
graph TD
    subgraph 认证与授权
        JWT[JWT Token<br/>Access + Refresh]
        RBAC[RBAC 权限<br/>admin/user/viewer]
        WS[工作空间隔离<br/>多租户数据隔离]
    end
    
    subgraph 数据安全
        ENC[AES-256 加密<br/>API Key 密文存储]
        HKDF[HKDF 密钥派生<br/>SECRET_KEY → 加密密钥]
        HASH[API Key 哈希<br/>bcrypt 不可逆存储]
    end
    
    subgraph 运行时安全
        RL[Rate Limiting<br/>Redis/内存 限流]
        SSRF[SSRF 防护<br/>URL 白名单校验]
        SB[沙箱执行<br/>Docker 隔离代码]
        SQL_INJ[SQL 注入防护<br/>关键词黑名单]
    end
    
    subgraph 传输安全
        HTTPS[HTTPS<br/>TLS 加密传输]
        CSP[CSP 头<br/>内容安全策略]
        CORS[CORS 锁定<br/>跨域白名单]
    end
```

---

## 六、应用启动流程

```mermaid
flowchart TD
    A[main.py<br/>入口文件] --> B[bootstrap.py<br/>初始化引导]
    
    B --> B1[检查/生成 SECRET_KEY]
    B --> B2[初始化数据库连接]
    B --> B3[自动建表 create_all]
    B --> B4[初始化向量存储]
    B --> B5[种子数据 model_seed]
    B --> B6[启动定时任务调度器]
    
    B --> C[http_setup.py<br/>HTTP 中间件]
    C --> C1[RequestIDMiddleware<br/>请求追踪]
    C --> C2[CORS 中间件]
    C --> C3[Rate Limit 中间件]
    C --> C4[安全响应头]
    
    B --> D[app_routes.py<br/>路由注册]
    D --> D1[挂载 24 个 API Router]
    D --> D2[静态文件服务]
    D --> D3[健康检查端点]
    
    B1 & B2 & B3 & B4 & B5 & B6 --> E[FastAPI 应用就绪]
    C1 & C2 & C3 & C4 --> E
    D1 & D2 & D3 --> E
    
    E --> F[Uvicorn 启动<br/>监听端口]
```

---

> 📌 **下一步**：阅读 `04-前端架构详解.md` 了解前端的组件设计与交互流程。
